import MainLayout from "@/components/layout";
import { Button } from "@nextui-org/react";

const HomePage = () => {
  return (
    <MainLayout>
      <Button>Click</Button>
    </MainLayout>
  );
};

export default HomePage;
